# Configuración del Backend - AB Gym Planner
# Copia este archivo como .env y ajusta según tu entorno

# Puerto del servidor
PORT=8000

# Host (0.0.0.0 para permitir conexiones externas, 127.0.0.1 solo localhost)
HOST=0.0.0.0

# Orígenes CORS permitidos (separados por coma)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Modo de desarrollo
DEBUG=true

# Nivel de logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
